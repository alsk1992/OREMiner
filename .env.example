# ORE Mining Environment Variables

# Your keypair (base58 encoded private key)
KEYPAIR=/path/to/keypair.json

# Your RPC endpoint (Helius recommended for reliability)
RPC=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY

# Command to run
COMMAND=deploy_optimal

# Optional: Custom deployment amount (in lamports)
# AMOUNT=40000000  # 0.04 SOL (default)
